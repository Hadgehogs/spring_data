spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres_test
    username: postgres
    password: 1
  jpa:
    show_sql: true
    format_sql: true
    hibernate:
      ddl-auto: create